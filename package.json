{
  "name": "tools",
  "version": "1.0.0",
  "private": true,
  "description": "A collection of useful web tools",
  "scripts": {
    "build": "node scripts/build-all.js",
    "build:drum-lesson": "cd apps/drum-lesson && npm ci && npm run build",
    "build:dnd-generator": "cd apps/dnd-generator && npm ci && npm run build"
  }
}
